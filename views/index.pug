extends layout

block content
  h1= title
  p Welcome to #{title}
  if user 
    p Hello, #{user.username}
    a(href="/logout") ログアウト
  else
    a(href="/login") ログイン  
  div#root
  script(defer, src="/javascripts/bundle.js") 

